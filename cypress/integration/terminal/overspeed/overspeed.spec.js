context.skip('超速设置', () => {
    before(() => {
    })

    beforeEach(() => {
    })

    after(() => {
    })

    afterEach(() => {
    })

    it('超速设置正确例子1', () => {
        expect(true).to.equal(true)
    })

    it('超速设置正确例子2', () => {
        expect(true).to.equal(true)
    })

    it('超速设置正确例子3', () => {
        expect(true).to.equal(true)
    })

    it('超速设置错误例子', () => {
        expect(true).to.equal(false)
    })
})
